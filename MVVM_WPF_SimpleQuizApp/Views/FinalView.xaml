<Window x:Class="MVVM_WPF_SimpleQuizApp.Views.FinalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM_WPF_SimpleQuizApp.Views"
        mc:Ignorable="d"
        Title="FinalView" Height="450" Width="800">
    <Grid VerticalAlignment="Stretch">
        <StackPanel HorizontalAlignment="Center">
            <Label Content="Congrats" VerticalAlignment="Center" FontSize="24" HorizontalAlignment="Center"></Label>
            <Label Content="{Binding Path=Username}" VerticalAlignment="Stretch" FontSize="20" HorizontalAlignment="Center"></Label>
            <Label Content="Your score:" FontSize="24" HorizontalAlignment="Center" Margin="0,100,0,0"></Label>
            <Label Content="{Binding Path=Score}" FontSize="24" HorizontalAlignment="Center" Margin="0,50,0,0"></Label>
            <Button Content="Exit" Command="{Binding Path=CloseWindow}" Margin="0,50,0,0" ></Button>
        </StackPanel>
    </Grid>
</Window>
